/*
 * app_sm.c
 *
 *  Created on: 26 sep. 2022
 *      Author: Santiago
 */

#include "app_sm.h"
#include "types.h"
#include "mpu9250.h"

State_t states;
MPU9250_t * data;

retType SM_Init(void){

	retType ret = API_OK;

	states = SM_INIT;
	return ret;
}

retType SM_Proccess(void){

	retType ret = API_OK;

	switch(states){
	case SM_INIT:
		ret |= DEV_InitI2C();
		if(ret != API_OK) return ret;
		ret |= APP_MPU9250Init();
		if(ret == API_OK) states = SM_READY2READ;
		break;
	case SM_READY2READ:
		ret |= APP_MPU9250ReadGyro(&data->gyro);
		ret |= APP_MPU9250ReadAccl(&data->accl);
		ret |= APP_MPU9250ReadTemp(&data->temp);
		if(ret == API_OK) states = SM_CONVERT;
		break;
	case SM_CONVERT:
		//Convert values
		if(ret == API_OK) states = SM_PRINT;
		else states = SM_READY2READ;
		break;
	case SM_PRINT:
		//print by UART
		if(ret == API_OK) states = SM_READY2READ;
		else states = SM_READY2READ;
		break;
	default:
		ret = API_ERROR;
		break;
	}
	return ret;

}
